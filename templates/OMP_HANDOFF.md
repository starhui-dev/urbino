# Urbino OMP 交接

实际日期、当前阶段/状态、最后成功动作、基线与当前代码revision：
当前修改与未跟踪文件：
已完成/失败/取消/仍活跃任务及实际ID：
文件所有权、未应用补丁与隔离目录：
实际执行命令及日志路径、NOT_RUN/SKIP/BLOCKED：
当前阻塞、已尝试修复及避免重复操作：
OMP版本/profile指纹/角色路由变化（不含秘密）：
下一步唯一动作或 /urbino-* 命令：

所有事实从磁盘与真实运行状态取得；不让会话摘要覆盖实际代码，不把待完成任务写成已完成。
