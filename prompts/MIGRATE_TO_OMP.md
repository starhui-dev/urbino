# 将已有工程增量迁移到 OMP

先确定用户提供的新版提示词包目录与实际目标仓库，两者不能猜测。阅读新版 AGENTS.md、MASTER_PROMPT.md、CHANGELOG.md、docs/17-omp-migration-recovery.md，再检查目标 Git diff、项目文件、进度、证据、已有 .omp/ 与活动规则。

1. 把新版包留在独立目录，先用 tools/migration_plan.py --target <实际仓库绝对路径> 生成只读差异计划。脚本不应用更新；禁止覆盖解压到目标。
2. 备份要改的非秘密文件并记录 hashes；源代码、真实配置、密钥、Git历史和数据库完全不动。绝不复制新版空 state或空证据去覆盖旧进度。
3. 合并仓库规则、OMP工作流、项目prompt/agents/skill、实际需要的辅助工具。现有同名 Agent/命令/规则冲突先保留用户自定义并做最小合并，不删除全局文件。
4. 正式名称统一为 Urbino，无中文产品名、无附加产品后缀；只改本项目标识，不改外部协议/字段/URL、缓存或账本历史数据。
5. 00–19阶段保持编号；将OMP委派/复核段追加到原阶段任务中，不丢弃用户扩充需求。保留原164测试行及已有执行结果，新增OMP检查独立管理。
6. 保留 state 中所有真实stage状态、证据路径和扩展字段，按实际更新 harness信息。受影响旧验收保留原件并标明需要重新验证，不能伪造旧会话信息或一键全verified。
7. 运行新版包结构/工具检查；迁移后的仓库不要求匹配发行MANIFEST，因为文件已合法改变。然后执行 OMP_SETUP 以核验实际模型与加载结果。
8. 写 progress/reports/omp-migration.md：变更/保留/冲突文件、备份路径、实际进度、已跑/未跑验证与下一步。迁移完成只接续当前阶段，不从00重做，不自动生产部署。
