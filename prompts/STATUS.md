# OMP 只读状态检查

只读 project.json、phases.json、progress/state.json、最新报告/交接、实际 Git diff 与已存在的 OMP runtime报告。汇总当前阶段、已实现/已验证范围、当前任务终态、阻塞、缺失证据和准确下一条 /urbino-* 命令。

本命令不运行产品测试、不创建/修改文件、不调用真实上游、不刷新秘密、不派写任务，不把历史的 implemented解释成 verified。报告无法验证的现状。
