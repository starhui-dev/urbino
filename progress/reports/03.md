# 阶段 03 · 阻塞修复执行报告

状态：implemented
代码 revision：`working-tree:source-tree-sha256:29630cda7c87790e41877eade7d0711699934c70b350d924698be0fff682223d`（未提交）
执行环境：Linux amd64；Go 1.27.1；显式 loopback PostgreSQL 测试数据库；2026-09-21

## 本轮修复

- `tests/identity/binding_identityimpl.go` 不再返回 `ErrPGAdapterPending`：真实 PostgreSQL adapter 负责迁移检查、seed tenant/project/user/member、digest-only API key 持久化、显式作用域列表/单项/统计和连接回收。
- `internal/storage/postgres/identity.go` 增加 project/user/member fixture 辅助、作用域列表/统计查询和统一未找到哨兵；所有资源查询保留 tenant/project 条件。
- `internal/storage/postgres/authenticator.go` 新增持久化认证器：public key/admin token 每次从 PostgreSQL 读取，加入 tenant/project/user/member 或 active admin principal 约束；撤销、停用和 bootstrap 状态由 PostgreSQL 共享，不使用进程内身份 map 作为事实来源。
- `internal/httpapi/auth.go` 将已验证 public/admin principal 放入请求上下文；`internal/httpapi/authservers.go` 装配独立 loopback public/admin listener，提供认证后的身份检查端点，拒绝跨 listener credential。
- `internal/cli/cli.go`、`internal/config/`、`api/config.schema.json` 接入 `public_addr`、`admin_addr`、受限 `auth_pepper_file` 及对应 `URBINO_` 环境覆盖；`serve` 保留 schema compatibility check，并在显式配置时同时启动 health/public/admin listener。
- `tests/identity/harness_test.go` 要求显式 `URBINO_TEST_POSTGRES_DSN`，不再猜测默认数据库目标，并在测试完成时关闭 adapter 连接。
- `docs/TEST_TRACEABILITY.md` 增加 P03-T01..T09 的实际测试和证据映射。

## 实际验证

| 命令/场景 | 结果 | 证据 |
|---|---|---|
| `go test ./... -count=1 -v` | 退出码 0；13 个包通过；无显式 DSN 的数据库门禁按约定声明 49 个 skip | `evidence/commands/phase03-go-test.txt` |
| `go vet ./...` | 退出码 0 | `evidence/commands/phase03-go-vet.txt` |
| 显式 loopback PostgreSQL：`go test -tags identityimpl ./tests/identity -run 'TestP03T0[23]' -count=1 -v` | 退出码 0；P03-T02/T03 共 7 项 PASS | `evidence/commands/phase03-scoped-store.txt` |
| `jq empty api/config.schema.json && make generate-check` | 退出码 0；生成与迁移副本无 diff | `evidence/commands/phase03-generate.txt` |
| 显式 loopback PostgreSQL：`go test -tags identityimpl ./tests/identity -count=2 -v` | 退出码 0；两轮 P03-T01..T09 全 PASS，无 SKIP/NOT_RUN | `evidence/commands/phase03-identity-full.txt` |
| 临时配置真实进程 smoke | bootstrap 独占输出成功；health=200；admin token 在 admin listener=200；同 token 在 public listener=401；admin 无凭据=401 | `evidence/commands/phase03-runtime-listeners.txt` |

## 当前边界与风险

- 本轮未运行生产 PostgreSQL、Valkey、River、真实 provider、完整管理资源 API、部署、备份恢复或升级回滚；不作超范围结论。
- public/admin listener 当前交付的是认证后的身份检查端点；完整 `/admin/v1` 资源管理 API 与完整 public data plane 属后续阶段，不能把本轮 smoke 解释为完整网关端到端通过。
- 当前工作树未提交；旧的 blocked review 针对修复前 revision，不能沿用。必须由新上下文 reviewer/security reviewer 复核当前 revision；本报告不标记 verified。

## 阶段边界

本轮完成阶段三可达阻塞修复，状态更新为 `implemented`，未自动进入阶段四、未提交、未部署。下一动作是按 `prompts/REVIEW.md` 对当前 revision 进行独立只读复核。
