# 阶段 03 · 执行报告

状态：blocked
代码 revision：`tree:f18b7347d2fa1519b3f90db924c8115f42fa9e93`（阶段状态仍为 blocked）
执行环境：Linux amd64；Go 1.27.1；2026-09-19

## 实际完成

- `internal/auth` 实现 public API key 与独立 admin token 的前缀、摘要、pepper、过期、撤销、scope/model policy 和一次性秘密输出。
- public/admin listener 认证路径分离；Authorization、`X-API-Key`、`X-Goog-Api-Key` 重复或冲突时拒绝；URL query 中的 key/token 参数拒绝；不信任转发头。
- 撤销缓存 TTL 上限 5 秒；缓存后端错误、miss、版本不一致 fail-closed；撤销状态通过共享后端传播。
- bootstrap 使用互斥状态保护；`internal/storage/postgres/admin.go` 使用 PostgreSQL advisory transaction lock、事务插入和 `O_EXCL` 0600 独占秘密文件。
- `migrations/0002_identity.sql` 及内置副本新增 project_members、api_keys、admin_principals、admin_tokens、audit_events，并使用 tenant/project 复合外键和状态约束。
- `internal/storage/postgres/identity.go` 提供显式 tenant/project scoped 的 tenant、user、member、API key 创建/查询/撤销存储路径。
- `tests/identity` 建立 P03-T01..T09 seam 与负向回归测试；测试中发现并修复了重复认证头、query key 名单不全、共享缓存撤销后 fail-open 三个缺陷。
- 修复 `internal/httpapi/auth.go` 管理 credential kind 判断，恢复全量构建。

## 实际验证

| 命令 | 结果 | 证据 |
|---|---|---|
| `go test ./... -count=1` | 退出码 0；项目包全通过；数据库集成子项因未设置显式 DSN 按约定 NOT_RUN/SKIP | `evidence/commands/phase03-go-test.txt` |
| `go vet ./...` | 退出码 0 | `evidence/commands/phase03-go-vet.txt` |
| `go build ./cmd/urbino` | 退出码 0 | `evidence/commands/phase03-go-build.txt` |
| `go generate ./api ./internal/storage/postgres && make generate-check` | 退出码 0 | `evidence/commands/phase03-generate.txt` |
| `go run ./cmd/urbino bootstrap --help` | 退出码 0；只输出 usage，无秘密 | `evidence/commands/phase03-cli-help.txt` |
| `go test -tags identityimpl ./tests/identity -count=1 -v` | 退出码 0；P03-T01、T04..T09 通过；P03-T02/T03 共 7 项因 ScopedStore PostgreSQL adapter 未绑定显式 SKIP | `evidence/commands/phase03-identity-linked.txt` |
| `go test -tags identityimpl ./tests/identity -count=2 -v` | 56 PASS / 14 SKIP / 0 FAIL；退出码 0 | `evidence/tasks/03/run-tagged-count2.txt` |
| `go test ./tests/identity -count=1` | 退出码 0；默认构建按约定显式 SKIP | `evidence/tasks/03/run-default-build.txt` |

## 测试映射

逐项映射见 `evidence/tasks/03/identity-boundary-tests.md`。P03-T02 的 4 项与 P03-T03 的 3 项没有 PASS 证据；它们要求显式 loopback PostgreSQL 以及真实 ScopedStore 绑定，不能按 SKIP 计算通过。

## 未验证项与风险

- P03-T02/T03 tenant/project persistence boundary 尚未实跑；当前 `tests/identity` 的 `NewScopedStore` 仍返回 `ErrPGAdapterPending`，所以本阶段不能标记 verified。
- 未运行真实 PostgreSQL 阶段三身份集成、Valkey、River、真实 provider、完整管理 API、部署、备份恢复、升级回滚；不作超范围结论。
- `internal/auth.Service` 当前主要是认证 seam；完整生产请求链与 PostgreSQL/Valkey 运行时装配属于后续整合边界，不能将测试内存 backend 当作生产部署证明。
- 迁移 SQL 根文件与内置副本已通过生成一致性检查，但未对新身份表执行真实 loopback PostgreSQL 集成命令。
- 独立 reviewer 与 security reviewer 发现：中间件此前只检查 credential 形状而未验证秘密（已修复为强制调用认证回调）；真实 listener/服务装配、持久化认证路径、ScopedStore、bootstrap 交叉故障语义仍未完成，不能把阶段标成 implemented/verified。
 
 ## 阶段边界
 
本轮保留 `blocked`；修复了认证中间件 fail-open 与 API key 成员资格边界后，仍需完成真实 public/admin listener 装配、PostgreSQL/Valkey 持久化认证、ScopedStore 和 bootstrap 集成，再重新取证复核。未进入阶段 04，未部署。

