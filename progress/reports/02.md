# 阶段 02 · 数据库、迁移与查询边界

状态：blocked（代码与单元验证已完成，等待隔离 PostgreSQL 环境后补齐集成门禁）。

## 已实现

- 引入锁定版本 pgx/v5 v5.11.0、goose/v3 v3.28.0；新增 `internal/storage/postgres` 连接池、显式 schema 兼容检查、statement/lock/idle-in-transaction 超时、短事务回滚与序列化失败重试。
- `migrations/001_initial.sql` 建立 Urbino schema、租户复合外键、请求/尝试/结算唯一键、价格版本、用量、预留和账本基础表；账本行设置不可变触发器与延迟平衡约束；runtime 角色仅获最小读取/写入授权。
- `Migrate` 使用嵌入 goose migration 和 PostgreSQL session advisory lock；`urbino migrate` 要求独立 DSN 文件、显式环境和预期数据库名。`serve` 只做 schema 兼容检查，不自动迁移。
- 新增 sqlc v1.31.1 配置、SQL 查询及生成仓储；新增 PG 测试 DSN 安全 helper 和错误脱敏单测；迁移操作 runbook。

## 验证

- `go test -count=1 ./...`：PASS。
- `go vet ./...`：PASS。
- `go build -o %TEMP%\\urbino-stage02.exe ./cmd/urbino`：PASS。
- sqlc v1.31.1 `generate`：PASS，生成文件在 `internal/storage/postgres/generated/`。
- `go test -race ./...`（CGO_ENABLED=1、MSYS2 gcc）：PASS。
- `git diff --check`：PASS。

## 未验证项与风险

- 本机未安装 Docker、psql，也没有经授权的隔离 PostgreSQL DSN；fresh/repeat/并行迁移、复合 FK 运行时拒绝、runtime 角色权限、取消回滚和 schema 升级兼容等真实集成门禁未运行，不能宣称通过。
- 迁移 Down 刻意不提供有损删除；生产回滚需按 runbook 采用应用优先的兼容回滚。
- `project.json`、`docs/14-naming.md` 在仓库不存在，沿用 AGENTS.md 与 `docs/00-scope.md` 命名契约。

下一步：执行 `prompts/REVIEW.md` 独立复核；取得明确隔离测试 PostgreSQL 后补跑 P02-T01 至 P02-T06 集成门禁。
