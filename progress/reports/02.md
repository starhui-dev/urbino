# 阶段 02 · 数据库、迁移与查询边界

状态：blocked（代码与本地验证已完成，等待隔离 PostgreSQL 环境后补齐集成门禁）。本次按 `prompts/REPAIR.md` 复核，未发现可在当前环境修复的实现回归。

## 已实现

- 引入锁定版本 pgx/v5 v5.11.0、goose/v3 v3.28.0；新增 `internal/storage/postgres` 连接池、显式 schema 兼容检查、statement/lock/idle-in-transaction 超时、短事务回滚与可取消的序列化失败重试。
- `migrations/001_initial.sql` 建立 Urbino schema、租户复合外键、请求/尝试/结算唯一键、价格版本、用量、预留和账本基础表；账本行设置不可变触发器与延迟平衡约束；runtime 角色仅获最小读取/写入授权。
- 新增版本化 `migrations/002_stage02_guards.sql`，不改写已存在的 001；补充租户币种复合 FK、价格发布后不可变边界并将 schema 版本推进到 2。
- `Migrate` 使用嵌入 goose migration 和 PostgreSQL session advisory lock；`urbino migrate` 要求独立 DSN 文件、显式环境和预期数据库名。`serve` 只做 schema 兼容检查，不自动迁移。
- 新增 sqlc v1.31.1 配置、SQL 查询及生成仓储；新增 PG 测试 DSN 安全 helper 和错误脱敏单测；迁移操作 runbook。
- 修复 usage event 查询遗漏必填 `tenant_id`；补充结算与 usage 的同租户/同请求复合外键、价格和账本不可变约束、非负用量约束；新增 `tests/pgtest/integration_test.go` 真实 PostgreSQL 集成测试入口（需显式隔离 DSN）。
- 本次修复补充 billing/journal 的租户币种复合外键，完善价格草稿与已发布版本的不可变边界；`RunTx` 在回调 panic、取消和回滚失败时保证回滚路径并返回脱敏错误。新增事务回归测试 `internal/storage/postgres/tx_test.go`。
- 集成测试已覆盖隔离库 fresh/repeat/并行迁移、跨租户 FK 与唯一性、runtime 无 DDL、账本/价格不可变、取消回滚和 schema 兼容；无 DSN 时显式失败，不再静默跳过。

## 验证

- `go test -count=1 ./...`：PASS。
- `go vet ./...`：PASS。
- `go build -o %TEMP%\\urbino-stage02.exe ./cmd/urbino`：PASS。
- sqlc v1.31.1 `generate`：PASS，生成文件在 `internal/storage/postgres/generated/`。
- 历史 `go test -race ./...`（CGO_ENABLED=1、MSYS2 gcc）证据属于修复前 revision，不能替代当前 revision 的 race 结果。
- `git diff --check`：PASS。
- `go test -tags integration -run '^$' ./tests/pgtest`：PASS（仅编译集成测试）。

本次修复复核（HEAD `171b569`）：

- `go test -count=1 ./...`、`go vet ./...`、`go build -o %TEMP%\\urbino-repair02.exe ./cmd/urbino`：PASS。
- `go test -tags integration -run '^$' ./tests/pgtest`、`git diff --check`：PASS。
- `go test -race ./...`：未通过，Go 报告当前环境未启用 CGO（退出码 2）；本机未发现可用 gcc，不能伪造 race 通过。
- `python tools/validate_pack.py` 与 `python tools/check_evidence.py`：未运行成功，Windows Python 别名返回命令不可用（退出码 9009）。
- 使用工作区绑定 Python 运行时执行 `tools/validate_pack.py`：PASS；`tools/check_evidence.py 02` 按预期报告 blocked、未通过项（退出码 1），未将其误记为通过。
- 生产修复后的最终 `gofmt`、`go test -count=1 ./...`、`go vet ./...`、`git diff --check` 与提示词包校验均 PASS（对应 `repair02-final2-*` 证据）。
- 价格项父版本加行锁并修正取消上下文下的回滚失败处理后，`final4` 全量测试、vet、集成编译和 diff 检查仍 PASS。
- `docker --version`、`psql --version` 与 PostgreSQL 服务检查：均不可用。

## 未验证项与风险

- 本机未安装 Docker、psql，也没有 PostgreSQL 服务或经授权的隔离 DSN；fresh/repeat/并行迁移、复合 FK 运行时拒绝、runtime 角色权限、取消回滚和 schema 升级兼容等真实集成门禁未运行，不能宣称通过。
- 迁移 Down 刻意不提供有损删除；生产回滚需按 runbook 采用应用优先的兼容回滚。
- `project.json`、`docs/14-naming.md` 在仓库不存在，沿用 AGENTS.md 与 `docs/00-scope.md` 命名契约。

下一步：取得明确隔离测试 PostgreSQL 后运行 `go test -tags integration ./tests/pgtest`，补跑 P02-T01 至 P02-T06 集成门禁并再次独立复核。
