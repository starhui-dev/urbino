# 阶段 00 · 工程基础与依赖锁定

状态：verified（已完成 `prompts/REVIEW.md` 独立复核）
代码 revision：HEAD `14ee4c3a0fa851457af62586302a2de6866f6478`；复核修复保留在工作树
执行环境：Windows amd64，Go 1.27.1，2026-09-12

## 实际完成

- 创建 `go.mod`/`go.sum`，module 为已确认 remote 对应的 `github.com/starhui-dev/urbino`，实际依赖仅为 chi v5.3.2。
- 创建 `cmd/urbino`，实现 `version`、`serve`、`help`；serve 默认只在 `127.0.0.1:9091` 暴露内部 `/healthz`，没有模型调用路由。
- 创建可注入 `internal/clock`、限制字段的 `internal/securitylog` 及对应测试。
- 创建 Makefile、GitHub Actions、`.gitignore`、`.dockerignore`、配置样例、依赖和参考 revision 锁定、ADR-0001。
- 未引入 CPA、CPA SDK、Sub2API 或其 module；提示词包文件未被清理。

## 验证

| 命令/测试 ID | 实际结果 | 退出码 | 证据路径 | 是否必要 |
|---|---|---:|---|---|
| `go test ./...` / P00-T01 | PASS | 0 | `evidence/raw/go-test.txt` | 是 |
| `go vet ./...` / P00-T01 | PASS | 0 | `evidence/raw/go-vet.txt` | 是 |
| `go build -o urbino.exe ./cmd/urbino` / P00-T01 | PASS | 0 | `evidence/raw/go-build.txt` | 是 |
| `go list -m all` / P00-T02 | PASS，无 CPA/S2A | 0 | `evidence/raw/go-list-modules.txt` | 是 |
| 配置/日志静态检查 / P00-T03 | PASS；样例无凭据或默认管理员口令，结构化错误日志不序列化错误正文 | 0 | `configs/urbino.example.yaml`、`internal/securitylog/securitylog_test.go` | 是 |
| version/unknown/health / P00-T04 | PASS；未知命令非零，health 200，模型路径 404 | 0/1（按命令） | `evidence/raw/cli-health.txt` | 是 |

## 未执行与阻塞

- `make`、`docker` 未安装，因此未执行 Makefile 和容器检查，记录为 NOT_RUN；等价 Go 命令已实际执行。
- 用户要求阅读的 `project.json`、`docs/14-naming.md` 在仓库中不存在，记录为输入缺失，未臆造内容。
- 阶段初始证据未记录提交 SHA；当前 HEAD 为 `14ee4c3a0fa851457af62586302a2de6866f6478`，模块路径依据现有 `origin` URL 确认。
- 完整依赖（PG/Valkey/River 等）只在锁定文档记录，尚未接入本阶段代码；其许可证/维护状态需在实际接入阶段再次核验。

## 名称契约

仓库 AGENTS.md、MASTER_PROMPT.md 和 docs/00-scope.md 规定正式产品名只能是 `Urbino`。代码、CLI 和健康响应均遵守该契约。

## 下一步

独立复核记录见 `progress/reports/00-review.md`；下一阶段为 01。
